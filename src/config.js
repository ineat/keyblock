const config = {

	blockchainUrl:"http://127.0.0.1:7545",
	keyManagerContractAddress = "",
	keyManagerAbiFile = "";

	key["public key here"] = "private key here"; //todo move it to a real vault
}

module.exports = config;